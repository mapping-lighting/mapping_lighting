<div class='observation-grid'>
  <%= datagrid_form_for @grid, :method => :get, :url => admin_observations_path %>
  <%= paginate(@grid.assets) %>
  <%= datagrid_table @grid %>
  <%= paginate(@grid.assets) %>
</div>
